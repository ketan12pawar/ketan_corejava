// String -: a string is a sequence of characters. It is represented by the String class, which is a built-in class in Java's standard library.
// Strings are widely used to store and manipulate textual data.
public class StringChar{
    public static void main(String[] args) {
        char ch[] = {'H','E','L','L','O'};
        String str = new String(ch);
        System.out.println(str); 
    }
}
